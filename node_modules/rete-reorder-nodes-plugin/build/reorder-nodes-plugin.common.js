/*!
* rete-reorder-nodes-plugin v0.3.0 
* (c) 2019 [object Object] 
* Released under the ISC license.
*/
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var index = {
  name: "reorder-nodes",
  install: install
};

function install(editor) {
  editor.view.on("nodeselected", function (node) {
    var nodeView = editor.view.nodes.get(node);

    if (nodeView == null) {
      throw new Error("Node view not found in node-to-view map");
    }

    var nodeElement = nodeView.el;
    var parentElement = nodeElement.parentElement;
    parentElement.lastElementChild.after(nodeElement);
    var index = editor.nodes.indexOf(node);

    if (index === -1) {
      throw new Error("Node not found in editor node list");
    }

    editor.nodes.splice(index, 1);
    editor.nodes.push(node);
  });
}

exports.default = index;
//# sourceMappingURL=reorder-nodes-plugin.common.js.map
